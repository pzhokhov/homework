# to run BC on Humanoid
python run_bc.py experts/Humanoid-v1.pkl Humanoid-v2 --num_rollouts=100 --expert-data humanoid_100.pkl

# to generate the MD table comparing expert and BC agents
for pkl in $(ls experts/); do env=${pkl%-v1.pkl}; python run_bc.py experts/${pkl} ${env}-v2 --num_rollouts=100; done 2>&1 >train.log | grep "|"
